
get / {
    return "Hello, World!"
}

get /tweets -> [schema.Tweet] {
    query {
        page: int
        page_size: int
    }

    let tweets = model.Tweet.get_by_user_id(user_id)?
    return tweets
}