// Extend from the BasicUserInfo schema
schema UserInfo(schema.BasicUserInfo) {
    @skip
    handle: str
    nickname: str
    gender: str
    avatar: str
    bio: str

    @computed
    fn handle() -> str {
        # Add @ prefix to the handle.
        return "@{handle}"
    }
}