class Outer {
  fn method() {
    print this; // expect: Outer {}

    fn f() {
      print this; // expect: Outer {}

      class Inner {
        fn method() {
          print this; // expect: Inner {}
        }
      }

      Inner().method();
    }
    f();
  }
}

Outer().method();
