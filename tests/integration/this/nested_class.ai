class Outer {
  fn method() {
    print(self); // expect: Outer {}

    fn f() {
      print(self); // expect: Outer {}

      class Inner {
        fn method() {
          print(self); // expect: Inner {}
        }
      }

      Inner().method();
    }
    f();
  }
}

Outer().method();
