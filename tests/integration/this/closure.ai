class Foo {
  fn getClosure() {
    fn closure() {
      return self.toString();
    }
    return closure;
  }

  fn toString() { return "Foo"; }
}

let closure = Foo().getClosure();
print(closure()); // expect: Foo
