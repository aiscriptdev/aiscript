enum Either {
    A = 1,
    B = 2,

    fn s() {
        print("static");
    }
}

Either.s(); // expect: static
Either.invalid(); // expect runtime error: Undefined static method 'invalid' of enum 'Either'.
