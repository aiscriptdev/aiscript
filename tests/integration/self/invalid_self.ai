class A {
    fn f(self="a", a) { // Error at '=': Expect parameter name.
    }
}

class B {
    fn f(self: A, a) { // Error at ':': Expect parameter name.
    }
}

class C {
    fn f(self a) { // Error at 'a': Expect ',' between 'self' and parameter.
    }
}