// Object literals are fine in initializer and condition parts
for let obj = {a:1}; len(obj) > 0; {  // This is OK - semicolons make it unambiguous
    print("hi"); // expect: hi
    break;
}

// But object literal in increment part should error
// for let i = 0; i < 10; {} {
//     print(i);
//     i += 1;
// }