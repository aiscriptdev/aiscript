class A {
  fn method(arg) {
    print "A.method(" + arg + ")";
  }
}

class B(A) {
  fn getClosure() {
    return super.method;
  }

  fn method(arg) {
    print "B.method(" + arg + ")";
  }
}


let closure = B().getClosure();
closure("arg"); // expect: A.method(arg)
