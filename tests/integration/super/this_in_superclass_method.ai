class Base {
  fn init(a) {
    self.a = a;
  }
}

class Derived(Base) {
  fn init(a, b) {
    super.init(a);
    self.b = b;
  }
}

let derived = Derived("a", "b");
print(derived.a); // expect: a
print(derived.b); // expect: b
