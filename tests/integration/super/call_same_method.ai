class Base {
  fn foo() {
    print("Base.foo()");
  }
}

class Derived(Base) {
  fn foo() {
    print("Derived.foo()");
    super.foo();
  }
}

Derived().foo();
// expect: Derived.foo()
// expect: Base.foo()
