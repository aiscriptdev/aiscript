class Base {
  fn foo() {
    print("Base.foo()");
  }
}

class Derived(Base) {
  fn bar() {
    print("Derived.bar()");
    super.foo();
  }
}

Derived().bar();
// expect: Derived.bar()
// expect: Base.foo()
