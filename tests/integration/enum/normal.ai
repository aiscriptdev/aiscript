enum HttpStatus {
    Ok = 200,
    NotFound = 404,
    ServerError = 500,

    pub fn as_str() -> str {
        return "as_str";
    }
}

let status = HttpStatus::Ok;
print(status); // expect: HttpStatus::Ok(200)
print(status.as_str()); // expect: as_str