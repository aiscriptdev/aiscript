let home = $HOME;
print(home.is_empty());       // expect: false
let h = "HOME";
print($h.is_empty());         // expect: true
print($(h).is_empty());       // expect: false
print($HOME == $(h));         // expect: true
print($INVALID.is_empty());   // expect: true
print($INVALID == "");        // expect: true
print($("HOME").is_empty());  // expect: false
print($("HO" + "ME").is_empty());  // expect: false
print($("INVALID").is_empty());// expect: true
