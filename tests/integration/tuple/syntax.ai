let empty = ();
print(empty); // expect: ()
print(() == ()); // expect: true
print(() == []); // expect: false

let x = (1);
print(x); // expect: 1

let one = (1,);
print(one); // expect: (1,)
print(one == 1); // expect: false

let a = (1, 2);
print(a); // expect: (1, 2)
print(a == a); // expect: true
let b = (1, 2,);
print(b); // expect: (1, 2)
print(a == b); // expect: true

print([1,2] == (1,2)); // expect: false

let a = "a";
let b = "b";
let c = "c";
print((a, b, c)); // expect: (a, b, c)
