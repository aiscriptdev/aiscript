route /poc {
    get /hello {
        query {
            @string(min_len=3, max_len=10)
            content: str
            aget: int = 18
        }
    }

    post /hello {
        query {
            @string(min_len=3, max_len=10)
            content: str
            aget: int = 18
        }

        @json
        body {
            test: bool = true
        }
    }
}